version: "3.8"

services:
  bark-server:
    image: finab/bark-server:latest
    container_name: bark-server
    restart: unless-stopped
    volumes:
      - ./data:/data
    ports:
      - "8087:8080"
    networks:
      - arm-net

networks:
  arm-net:
    external: true
