services:
  meilisearch:
    image: getmeili/meilisearch:latest
    container_name: meilisearch
    restart: unless-stopped
    ports:
      - '7700:7700'
    environment:
      - MEILI_ENV=production
      - MEILI_MASTER_KEY=meilimasterkey_secret_token
    volumes:
      - ./data:/meili_data
