# ==============================================================================
# Gitleaks Configuration — Zero-Secret Commits
# ==============================================================================

title = "Homelab Gitleaks Configuration"

[extend]
useDefault = true

[allowlist]
description = "Allowed placeholders and test fixtures"
regexes = [
  '''CHANGEME_.*''',
  '''generate_with_openssl_.*''',
  '''••••••••.*'''
]
