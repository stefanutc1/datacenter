services:
  web:
    build:
      context: ..
      dockerfile: web/Dockerfile
    container_name: homelab-digital-twin
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
