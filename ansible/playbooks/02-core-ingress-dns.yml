---
- name: Deploy Core Ingress & DNS Mesh
  hosts: lxc_x64
  become: true
  tasks:
    - name: Verify Nginx Proxy Manager and Pi-hole status
      command: docker ps
      when: inventory_hostname in ['nginx', 'pihole']
      changed_when: false
