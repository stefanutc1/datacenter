---
- name: Deploy Core Ingress & DNS Mesh
  hosts: lxc_x64
  become: true
  tasks:
    - name: Verify core services status
      command: docker ps
      when: inventory_hostname in ['immich', 'nextcloud']
      changed_when: false
