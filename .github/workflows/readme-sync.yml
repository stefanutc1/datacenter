name: Automated README & Inventory Sync

on:
  push:
    branches: [main, master]
    paths-ignore:
      - 'README.md'
  workflow_dispatch:

concurrency:
  group: readme-sync-${{ github.ref }}
  cancel-in-progress: false

jobs:
  sync-readme:
    name: Auto-Update README Metrics & Badges
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run README Auto-Sync Script
        run: |
          python3 scripts/sync_readme_metrics.py

      - name: Commit and Push Updated README
        run: |
          if [ -n "$(git status --porcelain README.md)" ]; then
            git config --local user.email "action@github.com"
            git config --local user.name "github-actions[bot]"
            git add README.md
            git commit -m "docs(readme): auto-update workloads, test stats and badges [skip ci]"
            git push
            echo "README.md auto-updated and committed."
          else
            echo "No changes detected in README.md."
          fi
